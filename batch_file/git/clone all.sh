#!/usr/bin/sh

BASEDIR=$(dirname "$0")
# 切換目錄
# cd "$BASEDIR"/libraries/

# 更新
# -- all
"$BASEDIR"/"clone hahahalib.sh"
"$BASEDIR"/"clone hahalib.sh"
"$BASEDIR"/"clone s_ulib.sh"
"$BASEDIR"/"clone g_plib.sh"
# 金融 X 法律 包，hahaha範圍內不允許直接使用此包在專案內，需另外隔開，避免 金融 & 法律 問題影響
# "$BASEDIR"/"clone _f_lib.sh"


# pwd
# read
