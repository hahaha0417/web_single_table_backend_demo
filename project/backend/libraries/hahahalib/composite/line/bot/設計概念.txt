由於linebot原先已經做好，所以不需要同樣東西再做一份
因此我這邊將一些寫好的設定包成獨立class，需要時new出來呼叫
不全部整合成一塊多層的class在一起是因為，可能有些功能不需要用，如果都new出來會變成require多個檔案，使的效能變慢

我這邊會提供一組寫好的設定，只要將設定貼過去，將要使用的東西取消註解，則可以使用
不用條件判斷處理require是因為我覺得將require寫在function內看起來怪怪的
